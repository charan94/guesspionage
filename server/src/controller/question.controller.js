import { Router } from 'express';

const router = Router();

router.get('/update/:userId', (request, response, next) => {

});


router.post('/find/:userId', (request, response, next) => {

});

export default router;
