import { Router } from 'express';

const router = Router();

router.get('/update/results/:userId', (request, response, next) => {

});


router.post('/find/results/:userId', (request, response, next) => {

});

export default router;
