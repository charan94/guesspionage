import { Router } from 'express';

const router = Router();

router.get('info/:userId', (request, response, next) => {

});


router.post('update/:userId', (request, response, next) => {

});


router.post('/create', (request, response, next) => {

});


router.get('/stats/find', (request, response, next) => {

});

router.get('/stats/find/:userId', (request, response, next) => {

});

export default router;